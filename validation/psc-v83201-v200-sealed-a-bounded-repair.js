(function(global){
'use strict';
const parent=global.QCSemanticCoreV69;if(!parent)throw new Error('V8.3.201 requires V8.3.200 V69');
const VERSION='V8.3.201-V200-V1-COMPOSITIONAL-GAP-REPAIR';
const fold=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/Đ/g,'D').toLowerCase().replace(/\s+/g,' ').trim();
const any=(s,a)=>a.some(x=>s.includes(x));
function frame(id,prev){const redirect=['input:prediction','input:hypothetical-or-example','input:decision-request'].includes(id),clarify=['input:third-party-only','input:clarification-required'].includes(id);return{...(prev||{}),id,action:redirect?'redirect':clarify?'clarify':'continue',must_stop:['input:prediction','input:decision-request'].includes(id),must_redirect:redirect};}
function neutral(d){const self=any(d,['i chose one proportionate action myself','i made one bounded decision on my own','i selected the practical step myself']);const done=any(d,['completed it','carried it out','finished it']);const closed=any(d,['left the matter closed','did not reopen the issue','stopped there']);return self&&done&&closed;}
function clarification(d){const own=any(d,['last observable action that belonged to me','what i personally did at the end','concrete response i myself made']);const missing=any(d,['has not been pinned down','is still absent','but not the concrete response']);return own&&missing;}
function decision(d){return any(d,['take over the judgement','take over my judgement'])&&any(d,['tell me exactly what to choose','tell me what to choose']);}
function hypothetical(d){return any(d,['invented case','invented test case'])&&any(d,['for testing only','test case'])&&any(d,['not an event from my life','not from my life']);}
function third(d){const hidden=any(d,['hidden assessment','secret view']);const other=any(d,['my colleague','other person']);const about=any(d,['of me','of me,','about me']);const unobs=any(d,['beyond anything i can directly observe','not anything about my own actions']);return hidden&&other&&about&&unobs;}
function prediction(d){return any(d,['likely to end positively for me'])&&any(d,['next few weeks','during the next few weeks']);}
function freeze(d){const reversible=any(d,['reversible experiment was ready','reversible experiment']);const excess=any(d,['kept adding alternatives','adding alternatives']);const delay=any(d,['instead of beginning it','instead of beginning']);return reversible&&excess&&delay;}
function ignore(d){const target=any(d,['important task required action','important task']);const avoid=any(d,['distracted myself with something peripheral','something peripheral']);return target&&avoid;}
function slow(d){const delayed=any(d,['answered later than usual','response came late','waited before replying']);const once=any(d,['reviewed it once','after one check','looked once more']);const close=any(d,['let the matter rest','did not reopen the issue','then moved on']);return delayed&&once&&close;}
function sequence(d){const toward=any(d,['moved toward acting']);const back=any(d,['stepped back']);const reenter=any(d,['re-entered the same review loop']);const repeat=any(d,['repeatedly without new evidence']);return toward&&back&&reenter&&repeat;}
function analyze(raw,domain='other',subtopic=null){const base=parent.analyze(raw,domain,subtopic),d=fold(raw);if(neutral(d)){const input_route=frame('input:self-lived',base.input_route);return{...base,version:VERSION,input_route,can_continue:true,must_stop:false,must_redirect:false,families:[],sequence:false,oscillation:false,response_known:false,canonical_shadow:{...(base.canonical_shadow||{}),difference_classification:'V8_3_201_V200_V1_COMPOSITIONAL_GAP_REPAIR'}};}let rid=null,fam=null;if(clarification(d))rid='input:clarification-required';else if(decision(d))rid='input:decision-request';else if(hypothetical(d))rid='input:hypothetical-or-example';else if(third(d))rid='input:third-party-only';else if(prediction(d))rid='input:prediction';else if(sequence(d))fam={families:['slow'],sequence:true};else if(freeze(d))fam={families:['freeze'],sequence:false};else if(slow(d))fam={families:['slow'],sequence:false};else if(ignore(d))fam={families:['ignore'],sequence:false};if(!rid&&!fam)return base;const route=rid||'input:self-lived',input_route=frame(route,base.input_route),families=rid?[]:fam.families,seq=rid?false:fam.sequence;return{...base,version:VERSION,input_route,can_continue:input_route.action==='continue',must_stop:!!input_route.must_stop,must_redirect:!!input_route.must_redirect,families:[...families],sequence:seq,oscillation:seq,response_known:families.length>0,canonical_shadow:{...(base.canonical_shadow||{}),difference_classification:'V8_3_201_V200_V1_COMPOSITIONAL_GAP_REPAIR'}};}
const core={...parent,version:VERSION,analyze,inputRoute:raw=>analyze(raw).input_route,schema:Object.freeze({...parent.schema,canonicalSemanticState:'css-proposal-v43-v201-v200-compositional-gap-repair'})};global.QCSemanticCoreV70=core;global.PSC_V83201=core;
})(typeof globalThis!=='undefined'?globalThis:this);
