name: V8.3.211 Preseal Construction Routed
on:
  push:
    branches: [v83211-v1-sealed-validation]
permissions:
  contents: write
jobs:
  preseal:
    if: github.event.head_commit.message == 'Run V8.3.211 preseal routed'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 1}
      - name: Materialize prior sealed references and V211 development challenge
        run: |
          mkdir -p validation/preseal_refs
          for v in 201 202 203 204 205 206 207 208 209; do s=${v#20}; git fetch --depth=1 origin refs/heads/v8320${s}-v1-sealed-validation:refs/remotes/origin/v8320${s}-v1-sealed-validation; git show origin/v8320${s}-v1-sealed-validation:validation/v8320${s}-v1-sealed/V8_3_${v}_SEALED_FIXTURE_V1.json > validation/preseal_refs/V8_3_${v}_SEALED_FIXTURE_REFERENCE.json; done
          git fetch --depth=1 origin refs/heads/v83210-v1-sealed-validation:refs/remotes/origin/v83210-v1-sealed-validation
          git show origin/v83210-v1-sealed-validation:validation/v83210-v1-sealed/V8_3_210_SEALED_FIXTURE_V1.json > validation/preseal_refs/V8_3_210_SEALED_FIXTURE_REFERENCE.json
          python validation/v83211-generalization-600-generator-v1r.py
          cp validation/V8_3_211_DEVELOPMENT_GENERALIZATION_600_V1.json validation/preseal_refs/V8_3_211_DEVELOPMENT_GENERALIZATION_600_REFERENCE.json
      - name: Generate and audit fresh V211 bank
        run: python validation/v83211-preseal-generator-v1r.py
      - name: Verify preseal authority
        run: |
          python - <<'PY'
          import json,pathlib
          root=pathlib.Path('validation/v83211-v1-sealed')
          a=json.loads((root/'V8_3_211_PRESEAL_DIVERSITY_AUDIT_V1.json').read_text());u=json.loads((root/'V8_3_211_SEALED_AUTHORITY_V1.json').read_text())
          assert a['pass'] and a['internal_max_similarity']<.75 and a['external_cases_at_or_above_0_75']==0 and a['exact_external_duplicates']==0
          assert not a['runtime_executed_during_bank_or_selection'] and not a['semantic_authority_loaded_during_bank_or_selection'] and not a['selection_uses_runtime_output']
          assert u['validated_development_head_sha']=='ec0646c126ffd60358013663c148f4ffd7080ca2' and u['semantic_authority']=='QCSemanticCoreV89' and not u['step_111_authorized']
          PY
      - name: Commit frozen preseal authority
        run: |
          python - <<'PY'
          import json,os,pathlib
          p=pathlib.Path('validation/v83211-v1-sealed');a=json.loads((p/'V8_3_211_PRESEAL_DIVERSITY_AUDIT_V1.json').read_text())
          (p/'V8_3_211_PRESEAL_RUN_RECEIPT.json').write_text(json.dumps({'candidate':'V8.3.211','phase':'preseal-construction','run_id':int(os.environ['GITHUB_RUN_ID']),'head_sha':os.environ['GITHUB_SHA'],'conclusion':'success','candidate_count':a['candidate_count'],'selected_count':a['selected_count'],'internal_max_similarity':a['internal_max_similarity'],'external_max_similarity':a['external_max_similarity'],'semantic_runtime_executed':False,'semantic_authority_loaded':False,'v210_sealed_rerun':False,'step_111_authorized':False,'production_authorized':False},indent=2))
          PY
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -- validation/v83211-v1-sealed
          git commit -m 'Freeze V8.3.211 V1 preseal authority'
          git push
