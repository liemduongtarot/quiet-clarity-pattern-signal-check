name: V8.3.142 Validation (superseded)

on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by v83142-validation-fixed.yml; no validation authority.'
