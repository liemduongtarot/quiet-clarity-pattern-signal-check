name: V8.3.247 transport/static preflight
# trigger-v247-static-preflight-1
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/v83243-actions-transport-probe.yml
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v83247-v1-sealed-validation
          fetch-depth: 0
      - name: Run V8.3.247 transport/static preflight
        id: run
        shell: bash
        run: |
          set +e
          python validation/v83247-v1-sealed/v83247-transport-preflight.py > /tmp/v247-preflight.log 2>&1
          rc=$?
          set -e
          cat /tmp/v247-preflight.log
          RC="$rc" python - <<'PY'
          import json,os,pathlib
          text=pathlib.Path('/tmp/v247-preflight.log').read_text(errors='replace')
          o=pathlib.Path('validation/v83247-v1-sealed')
          (o/'V8_3_247_TRANSPORT_PREFLIGHT_DIAGNOSTIC.json').write_text(json.dumps({'candidate':'V8.3.247','phase':'transport-static-preflight-diagnostic','run_id':os.environ['GITHUB_RUN_ID'],'rc':int(os.environ['RC']),'log_tail':text[-24000:],'semantic_runtime_executed':False,'batch_a_executed':False,'batch_b_executed':False},indent=2)+'\n')
          PY
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
      - name: Freeze V247 preflight evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add validation/v83247-v1-sealed/V8_3_247_TRANSPORT_PREFLIGHT_DIAGNOSTIC.json
          test ! -f validation/v83247-v1-sealed/V8_3_247_TRANSPORT_VERIFICATION_V1.json || git add validation/v83247-v1-sealed/V8_3_247_TRANSPORT_VERIFICATION_V1.json
          test ! -f validation/v83247-v1-sealed/V8_3_247_STATIC_SEALED_PREFLIGHT_V1.json || git add validation/v83247-v1-sealed/V8_3_247_STATIC_SEALED_PREFLIGHT_V1.json
          git diff --cached --quiet || git commit -m 'Freeze V8.3.247 transport/static preflight evidence'
          git push origin HEAD:v83247-v1-sealed-validation
      - name: Enforce preflight outcome
        run: test '${{ steps.run.outputs.rc }}' = '0'
