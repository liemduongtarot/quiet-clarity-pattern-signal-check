name: V8.3.253 hardened transport/static preflight
# trigger-v253-preflight-1
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/v83243-actions-transport-probe.yml
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: v83253-v1-sealed-validation
          fetch-depth: 0
      - name: Run hardened preflight
        id: run
        shell: bash
        run: |
          set +e
          python validation/v83253-v1-sealed/v83253-transport-preflight.py > /tmp/v253-preflight.log 2>&1
          rc=$?
          set -e
          cat /tmp/v253-preflight.log
          echo "rc=$rc" >> "$GITHUB_OUTPUT"
      - name: Freeze preflight evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          RC='${{ steps.run.outputs.rc }}' python - <<'PY'
          import json,os,pathlib
          o=pathlib.Path('validation/v83253-v1-sealed');text=pathlib.Path('/tmp/v253-preflight.log').read_text(errors='replace') if pathlib.Path('/tmp/v253-preflight.log').exists() else ''
          (o/'V8_3_253_TRANSPORT_PREFLIGHT_DIAGNOSTIC.json').write_text(json.dumps({'candidate':'V8.3.253','phase':'hardened-transport-static-preflight','run_id':os.environ['GITHUB_RUN_ID'],'rc':int(os.environ.get('RC') or 1),'log_tail':text[-30000:],'semantic_runtime_executed':False,'batch_a_executed':False,'batch_b_executed':False},indent=2)+'\n')
          PY
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add validation/v83253-v1-sealed/V8_3_253_TRANSPORT_PREFLIGHT_DIAGNOSTIC.json
          test ! -f validation/v83253-v1-sealed/V8_3_253_TRANSPORT_VERIFICATION_V1.json || git add validation/v83253-v1-sealed/V8_3_253_TRANSPORT_VERIFICATION_V1.json
          test ! -f validation/v83253-v1-sealed/V8_3_253_STATIC_SEALED_PREFLIGHT_V1.json || git add validation/v83253-v1-sealed/V8_3_253_STATIC_SEALED_PREFLIGHT_V1.json
          git diff --cached --quiet || git commit -m 'Freeze V8.3.253 hardened preflight evidence'
          git push origin HEAD:v83253-v1-sealed-validation
      - name: Enforce preflight outcome
        run: test '${{ steps.run.outputs.rc }}' = '0'
