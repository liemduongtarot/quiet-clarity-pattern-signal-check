name: V8.3.210 Development Generalization V2
on:
  push:
    branches: [v83210-v1-validation]
permissions:
  contents: write
jobs:
  generalization:
    if: github.event.head_commit.message == 'Run V8.3.210 development generalization V2'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 1}
      - uses: actions/setup-node@v4
        with: {node-version: '24'}
      - name: Materialize historical application authority
        run: |
          python - <<'PY'
          import zipfile
          with zipfile.ZipFile('PSC_V8_3_139_FINAL_LOCAL_READINESS_EXTERNAL_BUILD_PENDING_CHECKPOINT (1).zip') as z:z.extractall('.')
          PY
          test -f PSC_V8_3_138_DEV/public/psc-v3.js
      - name: Materialize frozen V209 sealed reference
        run: |
          mkdir -p validation/generalization_refs
          git fetch --depth=1 origin refs/heads/v83209-v1-sealed-validation:refs/remotes/origin/v83209-v1-sealed-validation
          git show origin/v83209-v1-sealed-validation:validation/v83209-v1-sealed/V8_3_209_SEALED_FIXTURE_V1.json > validation/generalization_refs/V8_3_209_SEALED_FIXTURE_REFERENCE.json
      - name: Generate same frozen V210 challenge
        run: python validation/v83210-generalization-challenge-generator.py
      - name: Run V83 extraction and classification audit
        id: challenge
        continue-on-error: true
        run: node validation/run-v83210-development-generalization-v2.cjs
      - name: Package V2 checkpoint
        if: always()
        run: |
          python - <<'PY'
          import pathlib,zipfile,hashlib,json,shutil,os
          st=pathlib.Path('V8_3_210_GENERALIZATION_V2_CHECKPOINT');st.mkdir(exist_ok=True)
          files=[pathlib.Path('validation/V8_3_210_DEVELOPMENT_GENERALIZATION_CHALLENGE_V1.json'),pathlib.Path('V8_3_210_DEVELOPMENT_GENERALIZATION_RESULTS_V2.json'),pathlib.Path('validation/qc-evidence-extractor-v2-v210-relational-semantics.js'),pathlib.Path('validation/psc-v83210-v210-semantic-rule-table-v5.js'),pathlib.Path('validation/run-v83210-development-generalization-v2.cjs')]
          for p in files:
            if p.exists():shutil.copy2(p,st/p.name)
          rp=pathlib.Path('V8_3_210_DEVELOPMENT_GENERALIZATION_RESULTS_V2.json');r=json.loads(rp.read_text()) if rp.exists() else None
          status={'candidate':'V8.3.210','phase':'development-generalization-v2','run_id':int(os.environ['GITHUB_RUN_ID']),'head_sha':os.environ['GITHUB_SHA'],'semantic_authority':'QCSemanticCoreV83','total':r.get('total') if r else None,'extraction_rate':r.get('extraction_rate') if r else None,'classification_rate':r.get('classification_rate') if r else None,'failure_attribution':r.get('failure_attribution') if r else None,'independence':r.get('independence') if r else None,'pass':bool(r and r.get('pass')),'sealed_validation_executed':False,'v209_sealed_rerun':False,'step_111_authorized':False,'production_authorized':False};(st/'V8_3_210_GENERALIZATION_V2_STATUS.json').write_text(json.dumps(status,indent=2))
          def sha(p):return hashlib.sha256(pathlib.Path(p).read_bytes()).hexdigest()
          (st/'SHA256_MANIFEST.txt').write_text(''.join(f'{sha(p)}  {p.name}\n' for p in sorted(st.iterdir()) if p.is_file()))
          z=pathlib.Path('PSC_V8_3_210_DEVELOPMENT_GENERALIZATION_V2_CHECKPOINT.zip')
          with zipfile.ZipFile(z,'w',zipfile.ZIP_DEFLATED) as q:
            for p in sorted(st.iterdir()):q.write(p,p.name)
          pathlib.Path('PSC_V8_3_210_DEVELOPMENT_GENERALIZATION_V2_CHECKPOINT_SHA256.txt').write_text(f'{sha(z)}  {z.name}\n')
          PY
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: PSC_V8_3_210_DEVELOPMENT_GENERALIZATION_V2_CHECKPOINT
          path: |
            PSC_V8_3_210_DEVELOPMENT_GENERALIZATION_V2_CHECKPOINT.zip
            PSC_V8_3_210_DEVELOPMENT_GENERALIZATION_V2_CHECKPOINT_SHA256.txt
      - name: Record V2 receipt
        if: always()
        run: |
          python - <<'PY'
          import pathlib,json,os
          p=pathlib.Path('V8_3_210_DEVELOPMENT_GENERALIZATION_RESULTS_V2.json');r=json.loads(p.read_text()) if p.exists() else None
          receipt={'candidate':'V8.3.210','phase':'development-generalization-v2','run_id':int(os.environ['GITHUB_RUN_ID']),'run_attempt':int(os.environ['GITHUB_RUN_ATTEMPT']),'head_sha':os.environ['GITHUB_SHA'],'status':'completed','conclusion':'success' if r and r.get('pass') else 'failure','total':r.get('total') if r else None,'extraction_passed':r.get('extraction_passed') if r else None,'classification_passed':r.get('classification_passed') if r else None,'failure_attribution':r.get('failure_attribution') if r else None,'semantic_authority':'QCSemanticCoreV83','sealed_validation_executed':False,'v209_sealed_rerun':False,'step_111_authorized':False,'production_authorized':False}
          pathlib.Path('validation/V8_3_210_GENERALIZATION_V2_RUN_RECEIPT.json').write_text(json.dumps(receipt,indent=2))
          PY
          git config user.name github-actions
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -- validation/V8_3_210_GENERALIZATION_V2_RUN_RECEIPT.json
          git commit -m 'Record V8.3.210 generalization V2 receipt'
          git push
      - name: Fail unless V2 passes
        if: always()
        run: |
          python - <<'PY'
          import json,pathlib,sys
          p=pathlib.Path('V8_3_210_DEVELOPMENT_GENERALIZATION_RESULTS_V2.json');r=json.loads(p.read_text()) if p.exists() else None
          if not (r and r.get('pass')):sys.exit(1)
          PY
