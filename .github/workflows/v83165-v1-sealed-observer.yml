name: V8.3.165 V1 Sealed Observer
on:
  workflow_run:
    workflows: ['V8.3.165 V1 Sealed Validation']
    types: [completed]
permissions:
  contents: write
jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Record sealed run evidence
        run: |
          mkdir -p validation
          cat > validation/V8_3_165_V1_SEALED_RUN_EVIDENCE.json <<EOF
          {
            "authority": "V8.3.165 V1 SEALED RUN EVIDENCE",
            "run_id": ${{ github.event.workflow_run.id }},
            "run_attempt": ${{ github.event.workflow_run.run_attempt }},
            "conclusion": "${{ github.event.workflow_run.conclusion }}",
            "head_sha": "${{ github.event.workflow_run.head_sha }}",
            "head_branch": "${{ github.event.workflow_run.head_branch }}",
            "step_111_authorized": false,
            "production_authorized": false
          }
          EOF
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add validation/V8_3_165_V1_SEALED_RUN_EVIDENCE.json
          git commit -m 'Record V8.3.165 V1 sealed run evidence' || exit 0
          git push
